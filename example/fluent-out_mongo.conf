<source>
  @type forward
</source>
<match mongo.*>
  @type copy
  <store>
    @type mongo2
    exclude_broken_fields a
    database 'test'
    collection 'fluent'
    mongo_log_level 'info'
  </store>
  <store>
    @type stdout
  </store>
</match>
